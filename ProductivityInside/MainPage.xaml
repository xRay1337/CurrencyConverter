<Page
    x:Class="ProductivityInside.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProductivityInside"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#0000AA" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >

    <Page.Resources>
        <ControlTemplate TargetType="Button" x:Key="btTemplate">
            <Border CornerRadius="25" BorderBrush="#55FFFF" BorderThickness="10" Background="#0000AA" Height="40" Width="100">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="Hello" />
            </Border>
        </ControlTemplate>
    </Page.Resources>
    <Grid Background="#0000AA" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>
        <ProgressRing Grid.Row ="0" x:Name="progressRing" IsActive="True" Width="60" Height="60" Foreground="#55FFFF"/>
        <Button Grid.Row ="1" Content="КОНВЕРТИРОВАТЬ" VerticalAlignment="Center" Height="100" Width="400" Foreground="#55FFFF" Background="#00AAAA" HorizontalAlignment="Center" Click="Button_Click"/>
    </Grid>
</Page>
